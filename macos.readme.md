## brew

```bash
brew tap haishanh/brew
brew install rmtrash

brew install zsh git hub wget curl gh the_silver_searcher jq
brew install iproute2mac
brew install diff-so-fancy
brew install ncdu # disk usage
brew install reattach-to-user-namespace
brew install httpie httpstat
brew install pgcli

# for git diff
brew install git-delta

go get -u github.com/nikitavoloboev/gitupdate

brew install sqlite-utils

# smart cd
# https://github.com/ajeetdsouza/zoxide
brew install zoxide

# line of code
brew install tokei

# "ls" replacement
brew install exa

# ascii art
brew install figlet
```

## cask

```bash
brew install --cask mos rectangle 
keepassxc snipaste
brew install --cask ticktick
# subl
brew install --cask sublime-text
# smerge
brew install --cask sublime-merge
# database
brew install --cask dbeaver-community
pgadmin4
azure-data-studio
authy

# dev
brew install --cask docker
sourcetree
dteoh-devdocs
# media
imageoptim
iina
# monitoring
# https://github.com/sveinbjornt/Sloth
sloth

# https://github.com/Kevin-De-Koninck/Clean-Me
# A small macOS app that acts as a system cleaner (logs, cache, ...)
brew install --cask clean-me

# other essential casks
dropbox slack-beta

# font
brew install --cask font-iosevka font-input
font-jetbrains-mono
font-jetbrains-mono-nerd-font
font-ubuntu-mono-nerd-font
font-ia-writer-mono

brew install homebrew/cask-fonts/font-{jetbrains-mono,iosevka,input}
```

### docker

setup docker hub mirrors

https://mirrors.ustc.edu.cn/help/dockerhub.html

## Key Remapping

```bash
cp me.haishan.KeyRemapping.plist ~/Library/LaunchAgents/com.example.KeyRemapping.plist
```
